import { LocaleDirective } from './locale.directive';

describe('LocaleDirective', () => {
  it('should create an instance', () => {
    const directive = new LocaleDirective();
    expect(directive).toBeTruthy();
  });
});
