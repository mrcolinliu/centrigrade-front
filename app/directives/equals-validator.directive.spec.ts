/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EqualsValidatorDirective } from './equals-validator.directive';

describe('Directive: EqualsTrueValidator', () => {
  it('should create an instance', () => {
    let directive = new EqualsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
