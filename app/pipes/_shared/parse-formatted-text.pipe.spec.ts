import { ParseFormattedTextPipe } from './parse-formatted-text.pipe';

describe('ParseFormattedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseFormattedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
